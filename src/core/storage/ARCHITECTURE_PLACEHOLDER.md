# src/core/storage

Обёртка над `@react-native-async-storage/async-storage` (для несекретных настроек: язык, тема, флаг disclaimer) и `expo-secure-store` (резерв на будущее).

Используется только через публичный API core-модуля. Прямой доступ к AsyncStorage из features запрещён.
