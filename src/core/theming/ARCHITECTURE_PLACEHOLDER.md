# src/core/theming

Design tokens (цвета, типографика, размеры) и логика переключения dark/light/auto.

Все цвета и размеры в проекте должны идти отсюда — `react-native/no-color-literals` и связанные ESLint-правила это enforce-ят.
